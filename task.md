# Multi-Bill PDF Separation & Upload Scaling Tasks

- [x] Increase multer upload limit to 50MB and update Gemini prompt/schema in `server.ts`
- [x] Update UI copy, file validation, and implement dynamic document forking in `src/pages/UploadBatch.tsx`
- [x] Verify build and TypeScript correctness with `npm run lint`
